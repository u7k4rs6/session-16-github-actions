# 03: First GitHub Actions Workflow

The `hello-actions.yml` workflow demonstrates a job with three steps:

1. Print a message
2. Print the current date
3. Print the runner operating system

Workflow files must be placed in `.github/workflows/` at the repository root for GitHub to discover them. This folder keeps the example next to the lesson.
